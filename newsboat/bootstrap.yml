# ==========================
# === Newsboat Bootstrap ===
# ==========================

config:
  dirs:
    - ~/.config/newsboat

  link:
    - ~/.config/newsboat: urls

  files:
    - ~/.config/newsboat/config: |
        miniflux-token "<api-key>"

        include "~/.cfg/newsboat/config"
