# =========================
# === GitHub CLI Config ===
# =========================

aliases:
  # repo aliases
  r:  repo
  rl: repo list
  rv: repo view
  re: repo edit

  # other aliases
  al:  auth login
  rc:  release create
  rpc: repo create

# preferred protocol
git_protocol: https
