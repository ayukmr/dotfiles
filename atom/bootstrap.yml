# ======================
# === Atom Bootstrap ===
# ======================

config:
  dirs:
    - ~/.atom

  files:
    - ~/.atom/styles.less: "@import '../.cfg/atom/styles.less';"

  link:
    - ~/.atom: config.cson
    - ~/.atom: keymap.cson
