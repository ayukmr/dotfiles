# ==========================
# === TaskWarrior Config ===
# ==========================

# ================
# === Settings ===
# ================

# data location
data.location = ~/.local/share/task

# vit report
report.vit.sort    = urgency-
report.vit.filter  = status:pending -WAITING limit:page
report.vit.labels  = ID,Acv,Age,Deps,P,Project,Tags,Recur,S,Due,Until,Description
report.vit.columns = id,start.age,entry.age,depends,priority,project,tags,recur,scheduled.countdown,due.relative,until.remaining,description

# priority urgencies
urgency.uda.priority.H.coefficient = 8
urgency.uda.priority.M.coefficient = 4
urgency.uda.priority.L.coefficient = 2

# due urgencies
urgency.user.tag.TOMORROW.coefficient = 8
urgency.user.tag.OVERDUE.coefficient  = 8

# aliases
alias.cal = calendar
alias.bdn = burndown
alias.burndown = burndown.daily

# disable week number
displayweeknumber = 0

# ==============
# === Colors ===
# ==============

# active time
color.active = green

# remove stripes
color.alternate =

# blocked
color.blocked  = yellow
color.blocking = bold black on yellow

# burndown
color.burndown.done    = bold black on green
color.burndown.pending = bold black on red
color.burndown.started = bold black on yellow

# calendar
color.calendar.due        = bold black on cyan
color.calendar.due.today  = bold black on yellow
color.calendar.holiday    = color8
color.calendar.overdue    = bold black on red
color.calendar.scheduled  = bold black on green
color.calendar.today      = bold black on blue
color.calendar.weekend    = color8
color.calendar.weeknumber = color8

# completed
color.completed = bold black on green

# deleted
color.deleted = bold black on red

# debug
color.debug = bold black on magenta

# due
color.due = cyan
color.due.today = yellow
color.overdue = red

# error
color.error = bold black on red

# footnote
color.footnote = yellow

# header
color.header = yellow

# history
color.history.add    = bold black on red
color.history.delete = bold black on yellow
color.history.done   = bold black on green

# scheduling
color.recurring = magenta
color.scheduled = green

# summary
color.summary.background =
color.summary.bar = bold black on green

# sync
color.sync.added    = bold green
color.sync.changed  = bold cyan
color.sync.rejected = bold red

# tags
color.tagged = blue
color.tag.next = blue

# priority
color.uda.priority.H = red
color.uda.priority.M = yellow
color.uda.priority.L = cyan
color.uda.priority.none = color8

# undo
color.undo.after = bold green
color.undo.before = bold red

# warning
color.warning = bold black on yellow
