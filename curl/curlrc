# ===================
# === Curl Config ===
# ===================

# retry three times on fail
retry = 3

# set 60 second timeout
connect-timeout = 60

# set previous url as referer
referer = ';auto'

# follow redirects
location

# use server-provided modification date
remote-time

# create directories for output file as needed
create-dirs
