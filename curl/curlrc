# ===================
# === Curl Config ===
# ===================

# follow redirects
location

# create directories as needed
create-dirs

# server-provided modification date
remote-time

# retry three times on fail
retry = 3

# sixty second timeout
connect-timeout = 60

# previous url as referrer
referer = ';auto'
