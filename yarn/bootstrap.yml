# ======================
# === Yarn Bootstrap ===
# ======================

packages:
  setup:
    shell:
      - yarn global upgrade: 'updating packages'

  packages:
    - yarn global add:
      # coffeescript
      - coffeescript

      # typescript
      - ts-node
      - typescript

      # tools
      - eslint
