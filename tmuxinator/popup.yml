# =========================
# === Tmux Popup Preset ===
# =========================

name: popup
root: ~/

# skip attaching
attach: false

windows:
  - shell:
    # disable status for session
    - tmux set status off
