# =======================
# === Cargo Bootstrap ===
# =======================

config:
  dirs:
    - ~/.cargo

  link:
    - ~/.cargo: config.toml

packages:
  packages:
    - cargo install:
      - evcxr_repl

    - cargo install --git:
      - https://github.com/XAMPPRocky/tokei.git tokei
