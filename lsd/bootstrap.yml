# =====================
# === LSD Bootstrap ===
# =====================

config:
  dirs:
    - ~/.config/lsd

  link:
    - ~/.config/lsd/config.yaml: config.yml
    - ~/.config/lsd/icons.yaml: icons.yml
