#!/usr/bin/env zsh

# ==================
# === Git Create ===
# ==================

# init repo
git init $@

# create github repo
gh repo create

# add files
git add .

# commit files
git commit -m 'Initialize repo'

# push upstream
git push -u origin main
