#!/usr/bin/env zsh

# ==================
# === Git Create ===
# ==================

# init repo
git init $@

# add files
git add .

# commit files
git commit -m 'Initialize repo'

# create github repo
gh repo create
