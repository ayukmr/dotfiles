#!/usr/bin/env zsh

# ==========================
# === Yarn Build and SCP ===
# ==========================

if [[ $# < 2 ]]; then
    print -P '%B%F{red}error%f%b: hostname and/or directory not given'
    exit 1
fi

yarn build &&
scp -r build "$1:$2"
