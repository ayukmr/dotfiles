# ===================
# === Git Aliases ===
# ===================

[alias]
    a = add
    p = push
    d = diff
    c = commit
    s = status -s
    l = log -n 20 --oneline

    sh = show
    pl = pull
    rs = reset
    cl = clone
    br = branch
    st = status
    ap = add -p
    hm = heatmap
    co = checkout
    ds = diffstat
    cm = commit -m
    dc = diff --cached

    psu = push -u origin main
    rsh = reset --soft HEAD~1
