# ===================
# === Git Aliases ===
# ===================

[alias]
    a = add
    p = push
    d = diff
    s = status -s
    l = log -n 20 --oneline

    sh = show
    rs = reset
    cl = clone
    br = branch
    st = status
    ct = commit
    ap = add -p
    hm = heatmap
    co = checkout
    ds = diffstat
    cm = commit -m
    dc = diff --cached

    psu = push -u origin main
    rsh = reset --soft HEAD~1
